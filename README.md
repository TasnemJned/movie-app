# Movie App 🎬

## About

This is a simple full stack movie app that I built using React, Node.js and MongoDB.
The goal of the app is to manage a list of movies where users can add, view, update and delete movies easily.

---

## Features

* Add a new movie
* View all movies
* Update a movie
* Delete a movie
* Filter movies by genre
* Loading and error handling in the UI
* Auto refresh every few seconds

---

## Technologies Used

* React (Vite)
* Node.js + Express
* MongoDB Atlas

---

## API Endpoints

* GET /api/movies
* POST /api/movies
* PUT /api/movies/:id
* DELETE /api/movies/:id
* GET /api/movies/:id/reviews

---

## How to Run the Project

### 1. Clone the project

```bash
git clone <your-repo-link>
cd movie-app
```

### 2. Install dependencies

```bash
npm install
cd client && npm install
cd ../server && npm install
```

### 3. Run the project

```bash
npm run dev
```

---

## Notes

This project helped me understand how frontend and backend connect together using APIs, how to work with MongoDB, and how to manage data between different collections.
